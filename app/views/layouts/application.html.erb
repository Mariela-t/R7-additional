<!DOCTYPE html>
<html>
  <head>
    <title>R7Additional</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="banner-background p-5 text-center">
  <h1>Customers and Orders</h1>
  <p>This is a simple application that associated orders with customers</p> 
  </div>
  
  <nav class="navbar navbar-expand-sm navbar-dark sticky-top">
    <div class="btn-group">
      <button class="btn dropdown-toggle" data-bs-toggle="dropdown" > Customers 
      </button>
      <div class="dropdown-menu">
      <a class="dropdown-item" href="/customers">Customer List</a>
      <a class="dropdown-item" href="/customers/new">New Customer</a>
      </div>
      </div>
    </div>
    <div class="btn-group">
      <button class="btn dropdown-toggle" data-bs-toggle="dropdown" > Orders 
      </button>
      <div class="dropdown-menu">
      <a class="dropdown-item" href="/orders/new">New Orders</a>
      <a class="dropdown-item" href="/orders">Order List</a>
      </div>
      </div>
    </div>
  </nav>
  <% if flash[:alert].present? %>
    <p class="alert alert-warning"><%= flash[:alert] %></p>
<% end %>
    <div class="container-fluid">
      <%= yield %>
    </div>
  </body>
</html>
